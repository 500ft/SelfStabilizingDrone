# Pinned after a clean isolated conda-forge install and a STEP export/reimport smoke test
# (20 x 10 x 5 box: volume 1000.000000 -> 1000.000000, relative difference 0). DR-CAD-10.
# cad/tests/test_geometry.py::test_installed_versions_match_lock asserts the running
# environment matches these lines, so CI cannot drift silently.
python==3.11.16
cadquery==2.8.0
cadquery-ocp==7.9.3.1
numpy==2.4.6
pytest==9.1.1
