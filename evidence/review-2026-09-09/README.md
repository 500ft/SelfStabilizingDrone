# CAD adversarial review amendment — 2026-09-09

Review base: `dc7810a2cbdb74bb0c580ea6c71f69d30a375aba` (day-2 PR head). Candidate
identity is the containing commit / PR head, not a self-embedded hash.
Scope: input admission and verification for the existing **motor envelope** family.
The day-1 input register and its owner gates remain unchanged.

## Verdict and priorities

- **P1 acceptance gap:** DR-CAD-10 was marked done even though its required geometry
  workflow existed only as an inert patch. It is now **in_progress** until the
  installed job runs green on the amended PR. The parent reviewer verified current
  workflow permission; the historical token limitation is not a current blocker.
- **P1 admission gap:** filled-in `pending` or empty evidence states generated
  geometry; duplicate rows silently overwrote previous values; NaN/infinity
  passed numeric parsing. The parser now rejects all three classes before build.
  An allowed state label is still not independent authentication of its source.
- **P2 verification gap:** the old lock test checked only Python and CadQuery.
  OCP, NumPy and pytest could drift unnoticed. All five explicit constraints are
  now tested, with the imported OCP version used when Conda omits Python package
  metadata. This is not a platform/build/transitive lock.
- **P2 round-trip gap:** the old round-trip test compared volume only. Synthetic
  changes to imported bounding dimensions or solid count passed. The round-trip
  gate now checks the full existing metric contract, including mass where defined.

Day 1's separation of sourced nominal dimensions, assumptions and unknowns is
useful and should stay. The limited day-2 family is a tooling demonstration, not
fixture CAD. Do not add fit dimensions or a prop swept thickness just to make the
model look more complete. Actual interfaces, metrology and containment remain
owner-gated.

## Reproduction

From this repository root, in the existing isolated CAD environment:

```sh
/Users/redhose/.claude-science/conda/envs/cadq/bin/python -m pytest cad/tests -q --tb=short
/Users/redhose/.claude-science/conda/envs/cadq/bin/python -m compileall -q cad
```

For another reviewer, install the five direct constraints with
`conda install -y --file cad/requirements.lock` in an isolated environment, then
run the same commands using that environment's Python. The installed workflow
uses this route; Linux/hosted installation is pending, not inferred from macOS.

- Original baseline: **12 passed**, exit 0.
- Tests added before implementation: **11 failed, 12 passed**, exit 1.
- After the correction: **23 passed**, exit 0. Seven upstream pyparsing
  deprecation warnings remain; none is a skipped test.
- Raw command output and exact case names are retained in [checks.json](checks.json).
  Regression inputs are generated by [test_geometry.py](../../cad/tests/test_geometry.py):
  NaN/inf/-inf in a required numeric cell; nonempty values with pending/empty state;
  duplicated first parameter; three monkeypatched dependency versions; and
  monkeypatched imported bbox/solid-count metrics. These are developer negative
  controls, not physical or independent evaluation cases.
- The original day-2 STEP files remain historical evidence. This amendment's tests
  generate and reimport fresh temporary artifacts; they do not overwrite the
  committed snapshots or claim bit-for-bit reproducibility of STEP serialization.

## Delivery and outstanding acceptance

Parent review also corrected the installer namespace: this is a Conda constraint
file, so its OCCT binding is `ocp`, not the pip distribution name `cadquery-ocp`.
The installed environment's `conda-meta/ocp-7.9.3.1-*.json` and
[conda-forge package](https://anaconda.org/conda-forge/ocp) establish that mapping.
Version checking still reads the actual imported OCP version. The earlier raw
regression names in checks.json retain the original pip-name label as history.

[Installed CAD workflow](../../.github/workflows/cad-geometry.yml) and the existing
analysis/evidence workflow now admit PRs to main or a `task/priority-one-*` base.
The CAD job consumes the constraint file directly, regenerates geometry, runs the
tests and uploads a source-SHA-named artifact. The old proposal remains in
`ci-proposed/` as an explicitly historical record; do not reapply it.

Hosted checks and workflow-permission evidence are recorded by the parent in the
cross-repository handoff after push. Until a real successful job is observed,
DR-CAD-10 stays in progress. No hardware, manufacturing release, safety approval,
measurement agreement or project validation verdict is authorized by these tests.
The sole task status is [CAD_TASKS.csv](../../docs/CAD_TASKS.csv).
